---
name: nine-grid-video
description: AI短剧自动制作工作流。通过九宫格故事版方法制作角色一致、风格统一、可连载的AI短剧。触发场景：(1) 制作AI短剧 (2) 生成九宫格故事版 (3) 首尾帧视频生成 (4) AI视频创作 (5) 短视频批量制作 (6) 故事分镜制作
---

# Nine Grid Video - AI短剧制作工作流

> **脚本路径说明**：本skill中的所有脚本位于项目目录下 `.claude/skills/nine-grid-video/scripts/`。

## 概述

通过"九宫格故事版"方法制作AI短剧，解决三大核心问题：
- **角色一致**：所有分镜来自同一张九宫格图，角色外观锁定
- **画风统一**：光线、色调、时代感天生一致
- **故事连贯**：九宫格本身就是短剧的骨架

## 分辨率规格

| 图片类型 | 目标分辨率 |
|---------|-----------|
| 九宫格故事版 | 2048x2048 (2K) |
| 放大后的分镜图 | 2048x2048 (2K) |

## 视频规格

| 参数 | 规格 |
|------|------|
| 视频模型 | Veo 3（云雾API） |
| 每个片段时长 | 8秒 |
| 目标平台 | 抖音/TikTok/小红书等短视频平台 |

## 可选步骤：对标分析（推荐）

> 💡 这一步能显著提升内容质量，强烈建议新手使用

如果用户有参考的爆款视频/脚本，可先进行拆解：

1. **分析脚本结构**：开篇如何抓眼球、中间如何铺垫、结尾如何收尾
2. **提取爆款技巧**：悬念设置、反转位置、升华方式
3. **总结叙事节奏**：每个分镜的信息密度、情感变化
4. **策划新选题**：基于拆解结果，策划5-10个同类选题

**爆款公式核心**：
- 开篇抛出困境/悬念（让观众不敢眨眼）
- 中间层层反转/颠覆认知（剧情紧凑）
- 结尾升华/留白（引发思考，让观众想看更多）

## 快速开始

用户提供以下信息即可开始：
1. **故事标题**：短剧的名称
2. **故事大纲**：想要包含的情节点（运用爆款公式：悬念→反转→升华）
3. **画面风格**：视觉风格描述（如：电影感、动漫风、写实等）
4. **画面比例**：9:16（竖屏）、16:9（横屏）或 1:1
5. **旁白语言**：中文、英文等
6. **发布平台**（可选）：抖音/TikTok/小红书等

**重要提示**：
- 每个分镜对应 **8秒视频**（Veo 3 模型）
- 每个分镜的旁白控制在 **35-50字**（中文），确保内容饱满不空洞
- 故事要紧凑、有信息量，适合短视频平台的快节奏

## 工作流程

### 第一步：生成九宫格提示词

根据用户的故事大纲，运用**爆款叙事公式**生成九宫格故事版的详细提示词：

```
创建一个3x3的九宫格电影分镜故事版，展示以下故事：
[故事大纲]

画面风格：[风格描述]

【爆款叙事结构】（每格对应8秒视频，内容要饱满）：
- K1-K2：开篇抛出困境/悬念，建立危机感
- K3-K5：层层反转，颠覆认知，剧情紧凑
- K6-K7：高潮时刻，关键揭示
- K8-K9：升华/留白，引发思考

要求：
1. 九宫格从左到右、从上到下编号为K1-K9
2. 主角外观（脸型、发型、服装）在所有9格中完全一致
3. 整体画面风格、色调、光线保持统一
4. 每格展示故事的一个关键时刻，禁止"过渡性"空镜头
5. K1-K9形成连贯的叙事顺序
6. 每格画面饱满，角色清晰可见，有明确的信息点或情感变化
7. 避免文字、对话框等元素
```

### 第二步：生成九宫格故事版图

运行脚本生成九宫格图片：

```bash
python .claude/skills/nine-grid-video/scripts/generate_storyboard.py \
  --prompt "提示词内容" \
  --style "画面风格" \
  --aspect-ratio "1:1" \
  --output ./output
```

### 第三步：拆分九宫格

将九宫格图片拆分为9张独立的分镜图：

```bash
python .claude/skills/nine-grid-video/scripts/split_grid.py \
  --input ./output/storyboard.png \
  --output ./output/frames
```

输出：K1.png 到 K9.png（低分辨率，作为参考）

### 第三步B：放大分镜图为 2K（推荐）

有两种方案可选：

#### 方案A：先裁切再放大（稳定）

先裁切九宫格，再用 Gemini 图生图放大每张小图：

```bash
python .claude/skills/nine-grid-video/scripts/upscale_frames.py \
  --input ./output/frames \
  --output ./output/frames_hd \
  --parallel 9
```

#### 方案B：直接从九宫格提取（推荐）

不需要预先裁切，直接让 Gemini 从九宫格中提取并放大指定格子：

```bash
python .claude/skills/nine-grid-video/scripts/extract_from_grid.py \
  --grid ./output/storyboard.png \
  --output ./output/frames_hd \
  --parallel 9
```

**两种方案对比**：

| 特性 | 方案A（先裁切再放大） | 方案B（直接从九宫格提取） |
|------|---------------------|------------------------|
| 裁切步骤 | 需要先运行 split_grid.py | 不需要 |
| API调用 | 9次（每张小图1次） | 9次（每个格子1次） |
| 上下文 | 只有小图本身 | 完整九宫格作为上下文 |
| 推荐场景 | 九宫格质量较差时 | 默认推荐 |

**输出**：`frames_hd/` 目录下的 K1.png 到 K9.png（2048x2048 高清版）

**说明**：
- 9张图并行生成，速度快
- 自动去除 K1、K2 等文字标记
- 保持角色和画风一致
- 后续视频生成会自动优先使用 `frames_hd` 目录

### 第四步：生成视频片段

使用分镜图作为首帧参考生成8个视频片段（云雾API veo_3_1模型）。

**默认并行模式**（推荐，速度快约8倍）：
```bash
python .claude/skills/nine-grid-video/scripts/generate_videos.py \
  --frames ./output/frames \
  --prompts ./output/video_prompts.json \
  --output ./output/videos
```

**自定义并行数**：
```bash
python .claude/skills/nine-grid-video/scripts/generate_videos.py \
  --frames ./output/frames \
  --prompts ./output/video_prompts.json \
  --output ./output/videos \
  --parallel 4
```

**串行模式**（调试用）：
```bash
python .claude/skills/nine-grid-video/scripts/generate_videos.py \
  --frames ./output/frames \
  --prompts ./output/video_prompts.json \
  --output ./output/videos \
  --parallel 1
```

并行生成分两个阶段：
1. **Phase 1**：同时提交所有8个视频生成任务
2. **Phase 2**：同时轮询所有任务状态，完成后下载

视频片段与分镜对应：
- scene_01.mp4: 以K1为首帧，提示词描述K1→K2的动作
- scene_02.mp4: 以K2为首帧，提示词描述K2→K3的动作
- ...
- scene_08.mp4: 以K8为首帧，提示词描述K8→K9的动作

视频提示词需要描述从当前帧到下一帧的动作过渡，确保生成的视频能自然衔接。

### 第五步：生成旁白

为每个分镜生成旁白文字（由Claude根据故事填写）：

**旁白长度要求**（适配8秒视频）：
- 中文：**35-50 字/分镜**
- 英文：**15-25 words/分镜**

**紧凑度要求**：
- 每句话必须有信息量，禁止"嗯、啊、那个"等填充词
- 旁白与画面同步，不能有"空等"
- 适合短视频平台的快节奏风格

```bash
python .claude/skills/nine-grid-video/scripts/generate_narration.py \
  --story "故事大纲" \
  --scenes 8 \
  --language "中文" \
  --output ./output/narration.json
```

### 第六步：生成配音

将旁白文字转为语音（ElevenLabs API）：

```bash
python .claude/skills/nine-grid-video/scripts/generate_audio.py \
  --narration ./output/narration.json \
  --voice "sam" \
  --output ./output/audio
```

### 第七步：合并最终视频

合并所有片段和配音（需要ffmpeg）：

```bash
python .claude/skills/nine-grid-video/scripts/merge_final.py \
  --videos ./output/videos \
  --audio ./output/audio \
  --output ./output/final.mp4
```

## 环境配置

需要在 `.env` 文件中配置：

```
GEMINI_API_KEY=your_gemini_api_key
YUNWU_API_KEY=your_yunwu_api_key
ELEVENLABS_API_KEY=your_elevenlabs_api_key
```

依赖安装：
```bash
pip install google-genai requests pillow python-dotenv elevenlabs
```

系统依赖：ffmpeg

## 成本参考

制作一支约1分钟的短剧：
- 图片生成（九宫格）：约 $0.09
- 分镜图放大（9张 2K）：约 $0.36
- 视频生成（8个片段）：约 $2-3
- 配音（11labs免费额度）：免费
- **总成本：约 $3-4**

## 输出文件结构

```
output/
├── storyboard.png           # 九宫格故事版 (2K)
├── frames/                  # （仅方案A需要）
│   ├── K1.png - K9.png      # 9张分镜图（裁剪版，低分辨率）
├── frames_hd/
│   ├── K1.png - K9.png      # 9张分镜图（2K 高清）
├── videos/
│   ├── scene_01.mp4 - scene_08.mp4
├── audio/
│   ├── scene_01.mp3 - scene_08.mp3
├── narration.json           # 旁白文字
├── video_prompts.json       # 视频生成提示词
└── final.mp4                # 最终合成视频
```

> **注意**：使用方案B（extract_from_grid.py）时，不需要运行 split_grid.py，也不会生成 frames/ 目录。

## 提示词编写指南

详见 [references/prompts_guide.md](references/prompts_guide.md)
